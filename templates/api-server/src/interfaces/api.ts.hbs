// This file is automatically generated and should not be edited.

{{! Request section}}
{{#each openapi.paths}}
  {{#each this}}
    {{#validMethodContext this @key}}
export interface {{paramOperation operationId}} {
{{> parseReqInterface this}}

{{! Handle the parameters at the method level (for path params)}}
{{#if ../parameters}}
{{> parseReqInterface ..}}
{{/if}}
}
    {{/validMethodContext}}
  {{/each}}
{{/each}}

{{! Schemas section}}

{{#each openapi.components.schemas}}
export interface {{@key}} {
{{> parseTypeSchema this}}
}

{{/each}}

{{! Responses w/o ref section}}
{{#each openapi.paths}}
  {{#each this}}
    {{#validMethodContext this @key}}
export interface {{resOperation operationId}} {
  status?: number
{{> parseTypeSchema responses.[200].content.[application/json].schema}}
}
    {{/validMethodContext}}
  {{/each}}
{{/each}}

{{! Responses w ref section}}
{{#each openapi.components.responses}}
{{#if [x-original-ref]}}
export interface {{parseRef [x-original-ref]}} {
  status?: number
{{#each content}}
    {{#each schema.allOf}}
{{> parseTypeSchema this}}
    {{/each}}
{{/each}}
}

{{/if}}
{{/each}}
